# MlangCast — Python dependencies
# Grouped by build-plan day so you only install what each stage needs.
requests>=2.31           # raw StatsBomb open-data JSON over HTTP
statsbombpy>=1.11
google-genai>=0.8
pymongo[srv]
python-dotenv>=1.0
google-cloud-texttospeech
flask>=3.0               # minimal web UI (web/app.py)
gunicorn>=21.0           # WSGI server used by the Cloud Run container