# MlangCast — Python dependencies
# Grouped by build-plan day so you only install what each stage needs.

# --- Day 1: data loader + go/no-go spike ---
requests>=2.31           # raw StatsBomb open-data JSON over HTTP
statsbombpy>=1.11
google-genai>=0.8
pymongo[srv]
python-dotenv>=1.0
google-cloud-texttospeech

# --- Day 2: replayer + commentary agent ---
# (no new hard deps — pure-Python replayer + agent reuse google-genai above)

# --- Day 3: MongoDB MCP context store
# pymongo>=4.6

# --- Day 4: text-to-speech + web UI
# google-cloud-texttospeech>=2.16
# elevenlabs>=1.0
# flask>=3.0
